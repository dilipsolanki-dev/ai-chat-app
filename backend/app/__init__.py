# This empty file makes `app/` a Python "package" so you can do `from app.main import ...`.
# (PHP has PSR-4 autoloading via composer; Python uses these __init__.py marker files
#  plus the directory name as the import path.)
